name: birthday_wishes
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  confetti: ^0.8.0
  url_launcher: ^6.0.20 # Added url_launcher dependency

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.2 # Added flutter_launcher_icons dependency
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
